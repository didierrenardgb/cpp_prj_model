#include <gtest/gtest.h>
#include <minefield/utils.h>
#include <minefield/types.h>

namespace freeFunc::tests
{
/*TEST(getStateValueFn, should_return_0_by_default)
{
    PositionState stateTest{};
    int expectedStateValue = Utils::getStateValue(stateTest);
    EXPECT_EQ(expectedStateValue, 0);
}*/

//TEST(enterNumberInRange, should_fail_number_out_of_range)
//{
//    std::stringstream input("25\n");
//    std::streambuf* originalCin = std::cin.rdbuf(input.rdbuf()); 
//
//    EXPECT_EQ(Utils::enterNumberInRange("test", 2, 10), 25);
//}

}
